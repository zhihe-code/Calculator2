<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.calculator2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Huilv"></activity>
        <activity android:name=".TimeCal" />
        <activity android:name=".Volume" />
        <activity
            android:name=".Length"
            android:label="@string/title_activity_length"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".JinZhi" />
        <activity
            android:name=".HistoryList"
            android:launchMode="singleTask" />
        <activity
            android:name=".Scientific"
            android:launchMode="singleTask"
            android:screenOrientation="reverseLandscape" />
        <activity
            android:name=".Normal"
            android:launchMode="singleTask"
            android:screenOrientation="reversePortrait" />
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>